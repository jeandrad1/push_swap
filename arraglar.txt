push_swap:
	leaks en los casos de argumentos por string. Hacer ft_free y llamarla para liberar todo el split.
checker:
	la función que comprueba el orden no funciona / da KO cuando está ordenada la pila
